Animation art page — design notes (animation.js)

A little theater. Register clips in the CLIPS list as video files are
added to /videos. The curtains open a beat after the page settles
(like house lights dimming before a show), the current clip loads
into the stage <video>, and the prev/next buttons step through CLIPS,
looping around at either end.
